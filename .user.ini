; Prepend the staging password gate to every PHP request.
; dev-gate.php only acts when the host is dev.argorobots.com, so this is a
; no-op on production even though the file is present in both document roots.
auto_prepend_file = "/home/argorobots/dev.argorobots.com/dev-gate.php"
